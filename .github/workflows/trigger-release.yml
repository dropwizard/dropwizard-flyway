---
# yamllint disable rule:comments rule:line-length
name: Trigger Release
# yamllint disable-line rule:truthy
on:
  workflow_dispatch:
    inputs:
      releaseVersion:
        description: Version of the next release
        required: true
        type: string
      developmentVersion:
        description: Version of the next development cycle (must end in "-SNAPSHOT")
        required: true
        type: string
jobs:
  release:
    uses: dropwizard/workflows/.github/workflows/trigger-release.yml@0ec34841b1d93de4fccd86a3a9112f331a65a01f # main
    secrets: inherit
    with:
      releaseVersion: ${{ inputs.releaseVersion }}
      developmentVersion: ${{ inputs.developmentVersion }}
